---
interface Technology {
  name: string;
  description: string;
  icon: string;
  category: string;
}

const technologies: Technology[] = [
  {
    name: "RAG (Retrieval Augmented Generation)",
    description: "Tecnología de IA que combina búsqueda de información con generación de lenguaje natural para chatbots inteligentes",
    icon: "🧠",
    category: "Inteligencia Artificial"
  },
  {
    name: "Text-to-Speech",
    description: "Conversión de texto a audio de alta calidad con voces naturales en español",
    icon: "🎤",
    category: "Multimedia"
  },
  {
    name: "SCORM",
    description: "Estándar internacional para contenido e-learning compatible con todas las plataformas LMS",
    icon: "📦",
    category: "Estándares"
  },
  {
    name: "Gamificación Avanzada",
    description: "Sistemas de puntos, badges, rankings y contenido desbloqueable tipo videojuego",
    icon: "🎮",
    category: "Engagement"
  },
  {
    name: "OpenAI Integration",
    description: "Integración con modelos de IA de última generación para procesamiento de lenguaje natural",
    icon: "🤖",
    category: "Inteligencia Artificial"
  },
  {
    name: "HTML5",
    description: "Estándar web moderno para crear contenido interactivo, multimedia y aplicaciones web avanzadas",
    icon: "🌐",
    category: "Frontend"
  }
];
---

<section class="py-20 bg-gray-50 relative overflow-hidden">
  <!-- Background Pattern -->
  <div class="absolute inset-0 opacity-5 pointer-events-none">
    <img src="/assets/patronblancobajaopacidad.png" alt="" class="w-full h-full object-cover" />
  </div>
  
  <div class="container mx-auto px-4 sm:px-6 lg:px-8 relative z-10">
    <div class="text-center mb-16">
      <h2 class="text-4xl md:text-5xl font-bold text-primary mb-6">
        🛠️ Con qué tecnologías trabajamos
      </h2>
      <p class="text-xl text-gray-600 max-w-3xl mx-auto mb-8">
        Utilizamos las tecnologías más avanzadas para crear soluciones e-learning de clase mundial
      </p>
      <div class="w-24 h-1 bg-accent mx-auto rounded-full"></div>
    </div>

    <!-- Technology Timeline -->
    <div class="max-w-5xl mx-auto relative mb-12">
      <!-- Timeline Line -->
      <div class="hidden md:block absolute left-1/2 transform -translate-x-1/2 w-1 h-full bg-gradient-to-b from-primary via-accent to-secondary"></div>
      
      <div class="space-y-12">
        {technologies.map((tech, index) => {
          const isEven = index % 2 === 0;
          const colors = [
            "from-primary to-accent",
            "from-accent to-secondary",
            "from-secondary to-yellow",
            "from-yellow to-primary",
            "from-primary to-accent",
            "from-accent to-secondary"
          ];
          const gradient = colors[index % colors.length];
          
          return (
            <div class={`relative flex items-center ${isEven ? 'md:flex-row' : 'md:flex-row-reverse'} flex-col gap-6 md:gap-8`}>
              <!-- Timeline Node -->
              <div class="relative z-10 flex-shrink-0">
                <div class={`w-16 h-16 md:w-20 md:h-20 rounded-full bg-gradient-to-br ${gradient} shadow-xl flex items-center justify-center text-3xl md:text-4xl group-hover:scale-110 transition-transform duration-300 border-4 border-white`}>
                  {tech.icon}
                </div>
                <!-- Connecting Line Dot -->
                <div class="hidden md:block absolute top-1/2 left-1/2 transform -translate-x-1/2 -translate-y-1/2 w-3 h-3 bg-white rounded-full border-2 border-primary"></div>
              </div>
              
              <!-- Content Card -->
              <div class={`flex-1 ${isEven ? 'md:text-right md:pr-8' : 'md:text-left md:pl-8'} text-center md:w-5/12`}>
                <div class="bg-white p-6 rounded-2xl shadow-lg border border-gray-100 hover:shadow-2xl transition-all duration-500 hover:-translate-y-1 group">
                  <div class={`mb-3 ${isEven ? 'md:justify-end' : 'md:justify-start'} flex justify-center`}>
                    <span class="text-xs font-semibold text-accent bg-accent/10 px-3 py-1.5 rounded-full">
                      {tech.category}
                    </span>
                  </div>
                  <h3 class={`text-xl md:text-2xl font-bold text-primary mb-3 group-hover:text-accent transition-colors duration-300 ${isEven ? 'md:text-right' : 'md:text-left'} text-center`}>
                    {tech.name}
                  </h3>
                  <p class={`text-gray-600 text-base leading-relaxed ${isEven ? 'md:text-right' : 'md:text-left'} text-center`}>
                    {tech.description}
                  </p>
                  
                  <!-- Decorative Element -->
                  <div class={`mt-4 h-1 bg-accent/30 rounded-full group-hover:bg-accent transition-all duration-300 ${isEven ? 'md:ml-auto' : 'md:mr-auto'} mx-auto md:w-20 w-10`}></div>
                </div>
              </div>
              
              <!-- Spacer for alternating layout -->
              <div class="hidden md:block flex-1 md:w-5/12"></div>
            </div>
          );
        })}
      </div>
    </div>

    <!-- Tech Stack Summary -->
    <div class="max-w-4xl mx-auto">
      <div class="bg-gradient-to-br from-primary/10 to-accent/10 rounded-2xl p-8 border border-primary/20">
        <h3 class="text-2xl font-bold text-primary mb-4 text-center">
          Stack Tecnológico Completo
        </h3>
        <div class="grid grid-cols-2 md:grid-cols-4 gap-4 text-center">
          <div>
            <div class="text-2xl mb-2">🤖</div>
            <p class="text-sm font-semibold text-gray-700">OpenAI</p>
          </div>
          <div>
            <div class="text-2xl mb-2">📦</div>
            <p class="text-sm font-semibold text-gray-700">SCORM</p>
          </div>
          <div>
            <div class="text-2xl mb-2">🎮</div>
            <p class="text-sm font-semibold text-gray-700">Gamificación</p>
          </div>
          <div>
            <div class="text-2xl mb-2">🌐</div>
            <p class="text-sm font-semibold text-gray-700">HTML5</p>
          </div>
        </div>
        <p class="text-center text-gray-600 mt-6 text-sm">
          Y muchas más tecnologías que nos permiten crear soluciones escalables, seguras y de alto rendimiento
        </p>
      </div>
    </div>
  </div>
</section>

<style>
  .container {
    max-width: 1200px;
    margin: 0 auto;
  }
</style>

